# rolls - kiss die roll example

To build application to target directory:

```sh
mvn compile
```

To run application after building:

```sh
mvn -q exec:exec
```

This creates a rolls.csv file with 100 die roll samples.

