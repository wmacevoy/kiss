# rolls - kiss rock paper scissors example

To build application to target directory:

```sh
mvn compile
```

To run application after building:

```sh
mvn -q exec:exec
```

This is a computer vs person rock-paper scissors console game.

